
import { useToast } from "@/hooks/use-toast";

/**
 * This component is kept as a placeholder since we've removed dark mode functionality.
 * It could be repurposed for other toggle needs in the future.
 */
const ThemeToggle = () => {
  return null;
};

export default ThemeToggle;
